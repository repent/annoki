- reply = false unless defined? reply
= form_for(comment) do |f|
  - if comment.errors.any?
    #error_explanation
      %h2
        = pluralize(@comment.errors.count, "error")
        prohibited this comment from being saved:
      %ul
        - comment.errors.full_messages.each do |message|
          %li= message
  = f.hidden_field :discussion_id, value: comment.discussion_id
  = f.hidden_field :parent_id, value: comment.parent_id
  .field
    - label = ( reply ? "Reply:" : "Your comment on the extract:" )
    = f.label :content, label
    = f.text_field :content
  .actions
    =# f.submit
